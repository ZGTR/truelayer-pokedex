{
  "name": "truelayer-pokedex",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "deploy:full": "sls create_domain --stage ${stage} --stage_rcs ${stage_rcs} && sls deploy -v --stage ${stage} --stage_rcs ${stage_rcs}",
    "deploy": "sls deploy -v --stage ${stage} --stage_rcs ${stage_rcs}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "serverless": "^3.1.1",
    "serverless-deployment-bucket": "^1.1.0",
    "serverless-domain-manager": "3.2.5",
    "serverless-dynamodb-local": "^0.2.38",
    "serverless-offline": "^8.4.0",
    "serverless-pseudo-parameters": "^2.6.1",
    "serverless-python-requirements": "^5.0.0",
    "serverless-wsgi": "^1.7.2"
  }
}
